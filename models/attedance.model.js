// add ka database mo

// const database = require("database.js")
// kunware database to, papasok dyan yung data

async function addAttendance(nfc) {
    const newStudent = {
        nfc,
    };
    console.log("MODELS", nfc);
}

module.exports = {
    addAttendance,
};
